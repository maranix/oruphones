import 'package:flutter/material.dart';

class OruPadding {
  static const kDefault = EdgeInsets.symmetric(horizontal: 12);

  static const kNarrow = EdgeInsets.all(8);

  static const searchBarInner = EdgeInsets.symmetric(
    horizontal: 12,
    vertical: 4,
  );

  static const searchBarOuter = EdgeInsets.symmetric(
    horizontal: 10,
    vertical: 8,
  );
}
